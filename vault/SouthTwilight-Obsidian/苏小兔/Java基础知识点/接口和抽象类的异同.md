一个没有抽象方法的抽象类从功能来看与接口相似。然而，需要注意如下区别:

+ 一个类可以实现多个接口，但却只能继承最多一个抽象类。
+ 抽象类可以包含具体方法;接口的所有方法都是抽象的。
+ 抽象类可以声明和使用字段;接口则不能，但可以创建静态的 fina1 常量。
+ 抽象类中的方法可以是 public、protected、private 或者默认的 package; 接口的方法都是 public。
+ 抽象类可以定义构造函数;接口不能。